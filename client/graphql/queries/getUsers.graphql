query GetUsers($first: Int = 10, $after: String = null) {
  users(first: $first, after: $after) {
    edges {
      node {
        slug
        nameWithCredentials
      }
      cursor
    }
    pageInfo {
      startCursor
      endCursor
      hasNextPage
      hasPreviousPage
    }
  }
}
