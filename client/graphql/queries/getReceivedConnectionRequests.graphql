query GetReceivedConnectionRequests($slug: String!) {
  user(slug: $slug) {
    pendingReceivedRequests {
      id
      createdAt
      requester {
        id
        slug
        nameWithCredentials
      }
    }
  }
}
