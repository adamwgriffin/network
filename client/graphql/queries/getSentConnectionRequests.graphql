query GetSentConnectionRequests($slug: String!) {
  user(slug: $slug) {
    pendingSentRequests {
      id
      createdAt
      recipient {
        id
        slug
        nameWithCredentials
      }
    }
  }
}
